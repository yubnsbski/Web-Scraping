# Reserved for a future per-role, multi-provider chain (see docs/brainstem.md
# O1/O2: local Ollama-first, escalate to Gemini). Not currently loaded by any
# code -- the codex_cli provider that used to read this file was removed
# (D2, owner ToS decision; see docs/brainstem.md section 5).
#
# NOTE: this repo's config loader does not support flow-style lists ([a, b]).
# Always use block-list syntax ("- item" lines) below, or loading fails loudly.
roles:
  critic:
    - gemini
  drafter:
    - gemini
  synthesizer:
    - gemini
